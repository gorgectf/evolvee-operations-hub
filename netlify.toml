[build]
    base = "frontend"
    command = "npm run build"
    publish = "dist"

[build.environment]
    NODE_VERSION = "20"

# Single-page app: send all routes to index.html so React Router works.
[[redirects]]
    from = "/*"
    to = "/index.html"
    status = 200
