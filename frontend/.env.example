# Leave empty for local development (the Vite dev server proxies /api to
# localhost:4000). For Netlify, set this to your backend URL, e.g.
# VITE_API_BASE=https://your-app.up.railway.app
VITE_API_BASE=
